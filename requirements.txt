fastapi>=0.111
uvicorn[standard]>=0.30
httpx>=0.27
pydantic-settings>=2.3
slowapi>=0.1.9
tzdata>=2024.1
pytest>=8.2

# V2 — collectors, local LLM pipeline
playwright>=1.44          # + run: python -m playwright install chromium
pillow>=10.0              # image handling for the vision stage
beautifulsoup4>=4.12      # readability/text extraction
lxml>=5.0
youtube-transcript-api>=0.6  # video transcripts (no quota, no OAuth)
reportlab>=4.0            # server-side PDF generation

# Instagram / social best-effort (method 2):
instaloader>=4.13
