fastapi==0.139.0
uvicorn[standard]==0.51.0
httpx==0.28.1
pydantic-settings==2.14.2
slowapi==0.1.10
tzdata==2026.2
pytest==9.1.1

# V2 — collectors, local LLM pipeline
playwright==1.61.0          # + run: python -m playwright install chromium
pillow==12.3.0              # image handling for the vision stage
beautifulsoup4==4.15.0      # readability/text extraction
lxml==6.1.1
youtube-transcript-api==1.2.4  # video transcripts (no quota, no OAuth)
reportlab==5.0.0            # server-side PDF generation

# Instagram / social best-effort (method 2):
instaloader==4.15.2
